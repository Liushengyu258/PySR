"""Post-processing: one script per paper figure.

Each module can be run directly, e.g.::

    python -m plotting.fig4_degradation

Every script reuses the shared matplotlib style from ``viz_config.py`` so
no figure rendering differs from the originals.
"""
